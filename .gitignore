*.swp
*.exe
.idea/
.claude/
.DS_Store
# Local-only: points at a sibling ../nanoda checkout while
# github.com/aethiopicuschan/nanoda/v2's 0.16.x support (PR #8) is
# unmerged. Never commit this — it references a path that only exists on
# this machine. Remove once the PR merges and go.mod's require points at
# a real tagged version instead.
go.work
go.work.sum

# Local dev env vars (KAG3_TRACE_TAGS etc., see .env.example) — loaded via
# github.com/joho/godotenv/autoload, personal to each machine.
.env
